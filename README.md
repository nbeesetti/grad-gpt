# grad-gpt

CSC 580 project
To run project:
First install gradio `pip install --upgrade gradio`
Then run `python app.py` to run the app on local server

```
source .venv/bin/activate
pip install -r requirements.txt
```