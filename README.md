# grad-gpt

## CSC 580 project
### To run project:
First install gradio `pip install --upgrade gradio`

```
source .venv/bin/activate
pip install -r requirements.txt
```

Then run `python gradGPT.py` to run the app on local server

Aditionally run `uvicorn notif_main:app --reload` to run the notifications backend

A `.env` file must also be created. Please reach out to developers to get this information.

### Created By:
Parshana Sekhon, Mio Nakagawa, Sinchana Shivaprasad, Neeraja Beesetti, Scott Pramuk